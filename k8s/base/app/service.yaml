apiVersion: v1
kind: Service
metadata:
  name: event-booking-web
  namespace: event-booking
spec:
  type: LoadBalancer
  selector:
    app: event-booking
  ports:
    - name: http
      port: 80
      targetPort: http
    - name: https
      port: 443
      targetPort: https
